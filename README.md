## 桌面助手数据格式

### 组内信息数据格式
- 存储位置：mysql+sqlite
- 组员信息
```json
{
	authcode:"cbb003",
	group:"CBB团队",
	isroot:true,
	name:"张三",
}
```
- 组内消息
```json
{
	category: "重要",
	content: "这是消息内容",
	group: "CBB团队",
	pin: false,
	time: "2026-02-03 10:00",
	is_delete: false
}
```

### 个人任务数据格式
```json
{
	task: "这是需要完成的任务",
	time: "2026-02-03 10:00",
    status:"未完成",
    priority:"重要",
    is_delete:false
}
```

### 型号管理数据格式
- 这个应该是nosql形式
```json

{

	field:"防务领域",
    series:"599大系列",
    General_model:"599",
    model:"599-1",
    version:"1.0",
    is_delete:false
}

```

```bash
请根据以下需求，生成完整的HTML网页。
要求：完全实现描述的功能，提供清晰易用的界面；界面整洁美观，排版规范，色彩和谐。
配色方案：
[卡片式ui，小清新的圆润风格，主题色白+淡蓝即可]
功能描述：
[型号管理应用，弹性盒子布局，初始分辨率800*600；我司的型号管理。整体结构为左右，右边又分为上下结构。首先由于我司型号分为系列->系列类别->具体型号系列->根据壳体号细分的型号，所以左边首先应该是一个树形可展开结构，最细分为具体型号系列。右侧的上半部分是该具体型号的总图jpg演示图，下半部分占比较大，为根据壳体号以及各个属性细分的具体型号，那在这一主要区域，包括结构化型号数据的展示，筛选，分类，查询，或者管理型号新增属性，可以拖动从而对型号的顺序进行调整，每一个型号都是nosql一条数据，型号数据都是通过electron通信传过来的，预留好接口，也可以测试内置一些型号数据。1 应该支持目录部分的拖动缩小的最小化（缩到最左边，整体位置都留给型号列表） 2 系列图片的展示给一个点击后在模态框放大展示的功能 3 右上角的系列概览调整下大小，新增按钮不能正常显示。 4 型号列表应该支持向右滑动，因为有的型号可能属性特别多  5 型号列表中，如果一列属性都为-(空)，那么这个属性就可以删除 6 型号应该支持编辑添加新属性和修改已有属性的值，表格还应该支持属性值筛选，可以做在表头上，弹出式选择筛选型]
必要要求：
[可以使用最终下载到本地的Tailwind css、Alpine.js Sortable.js库，不要使用最终需要联网的库，因为最终程序内网使用。]
```